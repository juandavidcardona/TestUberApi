# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TestUberApi' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TestUberApi
  pod 'UberRides', '~> 0.11'

  target 'TestUberApiTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TestUberApiUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
